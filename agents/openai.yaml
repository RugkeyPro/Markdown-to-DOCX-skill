interface:
  display_name: "Markdown to DOCX"
  short_description: "Convert Markdown to styled DOCX with Pandoc."
  default_prompt: "Use $markdown-to-docx to convert this Markdown article into a standardized DOCX with Pandoc."
